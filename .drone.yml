kind: pipeline
name: default

steps:
  - name: docker
    image: plugins/docker
    settings:
      username: kaddopur
      password:
        from_secret: docker_password
      repo: kaddopur/drone
      dry_run: true
      tags: latest
      build_args:
        - AUTH_KEY_ARG=http://yourproxy.com
